# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

# NOTE: This stage was built with the bindist Use flag enabled

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C.utf8

BINPKG_FORMAT="xpak"

GITHUB_BH_REPO="gentoo-klipper/binhost"

PORTAGE_BINHOST_HEADER_URI="https://github.com/${GITHUB_BH_REPO}/releases/download/binhost-${CHOST}"
PORTAGE_BINHOST="https://raw.githubusercontent.com/${GITHUB_BH_REPO}/binhost-${CHOST}"

FEATURES="${FEATURES} buildpkg binpkg-multi-instance getbinpkg"
                                                                  
